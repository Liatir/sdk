{
  "name": "liatir-sdk",
  "private": true,
  "description": "Public SDK packages for building, validating, and packaging Liatir plugins.",
  "type": "module",
  "workspaces": [
    "packages/liatir-core",
    "packages/liatir-api",
    "packages/liatir-cli"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm test --workspaces"
  }
}