{
  "name": "@liatir/cli",
  "version": "1.9.11",
  "description": "CLI for developing and building .lia plugins & WASM custom tools for Liatir",
  "bin": {
    "liatir": "dist/cli.js"
  },
  "type": "module",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "npm run build --prefix ../liatir-core && node scripts/clean-dist.js && tsc -p tsconfig.json && node scripts/make-executable.js && node scripts/copy-assets.js",
    "dev": "node scripts/copy-assets.js && tsc -p tsconfig.json --watch",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "esbuild": "^0.24.0",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
